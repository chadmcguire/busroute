This is a simple java application used to calculate but time valences from a spreadsheet input.
Given a spreadsheet of bus routes and times this will use a valence calculation to calculate the devation in the bus routes.
The application requires a CSV to load and then will export the results to a new CSV
